introspection:
  schemaFile: ./api.graphqls

info:
  title: KIV/PIA Labs GraphQL API
  description: |
    GraphQL schema for the KIV/PIA Labs project.
    
    Export HTML docs using [spectaql](https://github.com/anvilco/spectaql):
    
    ```sh
    npx spectaql --quiet --one-file --target-dir . --target-file api.html spectaql.yaml
    ```

servers:
  - url: http://localhost:8080/pia-labs/spring/graphql
    description: PIA Labs
